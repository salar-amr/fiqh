'use strict';

/**
 * registeration-rule service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::registeration-rule.registeration-rule');
