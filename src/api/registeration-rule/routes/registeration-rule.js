'use strict';

/**
 * registeration-rule router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::registeration-rule.registeration-rule');
